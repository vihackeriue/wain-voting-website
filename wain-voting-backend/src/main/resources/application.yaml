server:
  port: 8080
  servlet:
    context-path: /voting
spring:
  datasource:
#    url: ${DBMS_CONNECTION:jdbc:mysql://localhost:3306/demo_data} #lấy biến môi trường, nếu không có thì lấy giá trị mặt định
#    username: ${DBMS_USERNAME:root}
#    password: ${DBMS_PASSWORD:root}
#    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DBMS_CONNECTION:jdbc:mysql://localhost:3306/demo_data} #lấy biến môi trường, nếu không có thì lấy giá trị mặt định
    username: ${DBMS_USERNAME:root}
    password: ${DBMS_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true